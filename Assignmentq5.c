//WAP to calculate the length of String using printf function.
#include<stdio.h>
#include<string.h>
int main(){
    int count;
    char m[10]="hello";
    count=strlen(m);
    printf("the length of string is :%d",count);
    return 0;

}

